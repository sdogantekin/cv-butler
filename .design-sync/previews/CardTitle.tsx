import { Card, CardHeader, CardTitle } from "cv-butler";

export function InContext() {
  return (
    <Card style={{ width: 260 }}>
      <CardHeader>
        <CardTitle>Cover Letters</CardTitle>
      </CardHeader>
    </Card>
  );
}
