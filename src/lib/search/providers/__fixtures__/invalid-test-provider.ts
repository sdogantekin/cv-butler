// Test fixture only — deliberately does not export a CompanyResearchProvider,
// exercising company-search.ts's graceful-failure path. Not used by the app.
export const somethingElse = "not a provider";
